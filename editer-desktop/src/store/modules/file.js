const state = {
  files: [
    { title: "这个是一个很长的名字也是第一个测试文件名" },
    { title: "this_is_a_test" }
  ]
};

const getters = {};
const actions = {};
const mutations = {};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
};
